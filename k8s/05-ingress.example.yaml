apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tars-ingress
  namespace: tars
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    traefik.ingress.kubernetes.io/router.entrypoints: "websecure"
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  tls:
    - hosts:
        - tars.example.com # Replace with your real domain (e.g. tars.duckdns.org or yourdomain.com)
      secretName: tars-tls-secret
  rules:
    - host: tars.example.com # Replace with your real domain
      http:
        paths:
          # Backend API routes
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: tars-backend
                port:
                  number: 8000
          # Backend health check
          - path: /health
            pathType: Prefix
            backend:
              service:
                name: tars-backend
                port:
                  number: 8000
          # Frontend SPA application (root and all other routes)
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tars-frontend
                port:
                  number: 80
